<ul>
    {{#each profile}}


    <li>
        <p>{{this.first_name}} {{this.last_name}}</p>
        <img class="picformat" src={{this.photo}} alt="ProfilePic">
        <button data-profilematchid="{{this.id}}" data-pactivity="{{this.primary_training_type}}"
            class="gitfit-match">Accept</button>
        <button data-profilematchid="{{this.id}}" data-pactivity="{{this.primary_training_type}}"
            class="gitfit-match">Block</button>
    </li>


    {{/each}}
</ul>